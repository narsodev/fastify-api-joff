{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "dev": "lerna run dev"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
